# Constrain dev dependencies to the exact pins from the base lockfile
# to avoid pip resolution conflicts when installing both files together.
-c requirements.txt

markupsafe==3.0.3 # Override conflict with requirements.in
typing-inspection==0.4.2 # Override conflict with requirements.in (NEW FIX)

pytest
pytest-xdist
pytest-django
django-tenants
pytest-cov
factory-boy
ruff
black
pip-tools
PyMuPDF
pdfplumber
pikepdf
langdetect>=1.0.9,<2.0
markdown-it-py<4
mdit-py-plugins
python-docx
python-pptx
readability-lxml
openapi-python-client
charset-normalizer==3.4.4 # unify with prod to avoid pip resolution conflict
langfuse>=2.0.0 # optional SDK for local tracing
