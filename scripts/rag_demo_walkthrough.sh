#!/usr/bin/env bash
# DEMO/INTERNAL; deprecated for MVP.
#
# The RAG demo workflow has been removed from the MVP build. The script is kept
# only for historical reference and now exits immediately.
set -euo pipefail

echo "[rag-demo] This walkthrough is deprecated and no longer supported in the MVP build." >&2
exit 1
